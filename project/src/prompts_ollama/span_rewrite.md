You are a requirements rewriter.
Using the labeled spans, rewrite the requirement to be clearer and less ambiguous.
Return ONLY plain text (1-2 sentences).

Guidelines:
- Make the Main_actor explicit.
- Move Conditions to the beginning ("When/If ...").
- Keep Constraints measurable and explicit.
- Preserve meaning; do NOT add new requirements.

Original requirement:
{{REQUIREMENT_TEXT}}

Labeled spans JSON:
{{SPANS_JSON}}
